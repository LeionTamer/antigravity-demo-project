{
  "name": "monorepo-app",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "dev:backend": "bun --filter backend dev",
    "dev:frontend": "bun --filter frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
