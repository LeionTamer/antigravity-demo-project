# Project Rules

- **Package Manager**: This is a **Bun** project. Always use `bun` or `bunx` instead of `npm`, `yarn`, or `npx`. **NEVER use `npx`**.
- **Lockfile**: `bun.lock` is present.
- **Database Naming**: Table fields should be `camelCase` (e.g., `createdAt`, `updatedAt`, `writingStyle`).
